# 05_ARCHIVE — quarantine and historical copies

| Folder | What | Status |
|---|---|---|
| `_removed/` | Quarantined duplicates and one defective rendering (IDs Q-STMT-DUP, Q-PORTRAIT, Q-TL-PNG, Q-TL-WEBP). Verification details: MANIFEST.md `_removed` section and DOCUMENT-MAP. | Retained deliberately as provenance artifacts. MANIFEST says "delete when satisfied" — deletion remains the owner's call. |
| `dekun-property-export/` | 2026-08-12 export of the claude.ai project (git-versioned). | Partially stale (see D-6): its README and reconciliation doc predate the statement reading. Still-current content inside: `PROJECT.md` — the analysis charter (ID EXP-PROJECT), which remains the project's operating doctrine; and the pdf-text extracts (grep-friendly text layers of CO1/CO2/CO3 and findings). The export's SFPAY images are byte-identical corroborating copies; its CSLB image is a pixel-identical re-encode (D-4, resolved). |

Nothing in this tree is cited as evidence except through its DOCUMENT-MAP entry.
